<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_partner_form_estate_crm" model="ir.ui.view">
        <field name="name">res.partner.form.estate.crm</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="priority" eval="20"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='category_id']" position="after">
                <field name="is_property_owner" widget="boolean_toggle"/>
                <field name="is_allied_agency" widget="boolean_toggle"/>
            </xpath>
        </field>
    </record>
</odoo>
